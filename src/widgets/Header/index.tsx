import { Container, Logo } from "./styles";

const Header = (props: any) => {
  return (
    <Container>
      <Logo>
        <span>Sweeter</span>
      </Logo>
    </Container>
  );
};

export default Header;
